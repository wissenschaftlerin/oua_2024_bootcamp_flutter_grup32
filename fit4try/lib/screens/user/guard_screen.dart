import 'package:flutter/material.dart';

class GuardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Guard')),
      body: Center(child: Text('Guard Page Content')),
    );
  }
}
